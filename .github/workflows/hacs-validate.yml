name: HACS Validate

on:
  push:
  pull_request:

jobs:
  hacs:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v5
      - name: HACS Validation
        uses: "hacs/action@main"
        with:
          category: plugin
